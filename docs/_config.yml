# GitHub Pages Configuration for TenangDB

# Site settings
title: "TenangDB - Backup yang Bikin Tenang"
description: "Automated MySQL & PostgreSQL backup solution with smart confirmations, cloud upload, and monitoring. Deploy in seconds with Docker."
url: "https://tenangdb.ainun.cloud"
baseurl: ""

# Repository
repository: "abdullahainun/tenangdb"

# Author
author:
  name: "Ainun Abdullah"
  email: "abdullah.ainun4@gmail.com"
  url: "https://github.com/abdullahainun"

# Social
github_username: abdullahainun

# Build settings
markdown: kramdown
highlighter: rouge

# GitHub Pages settings
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag

# Exclude files from processing
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor

# Include files
include:
  - _pages

# Collections
collections:
  docs:
    output: true
    permalink: /:collection/:name/

# Defaults
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"
  - scope:
      path: ""
      type: "docs"
    values:
      layout: "docs"

# SEO
google_analytics: # Add your GA tracking ID if needed
google_site_verification: # Add your verification code if needed

# Theme
remote_theme: # Leave empty for custom theme
